import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PageUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Trang chi tiet',
  },
});
